# Synthetic training data

The model training script generates 1,800 deterministic synthetic questionnaire rows (seed `26094`). It contains no patient, victim, or personally identifying data. Each feature uses a 1–5 response scale where higher values mean greater concern. Labels are demonstration-only screening categories derived from a weighted synthetic burden score with noise.
